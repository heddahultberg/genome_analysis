# Data Directory

This folder will contain all data used in the project.  
Raw sequencing data is stored as **soft links** and not tracked by Git.

## Structure
- `raw_data/` – symbolic links to raw FASTQ/FQ files from UPPMAX  
- `meta_data/` – metadata files such as sample descriptions or run information

